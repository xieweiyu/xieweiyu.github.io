$(function() {    var height1;    var height2;    var height3;    var height4;    var h;    $('img').load(function() {        var box = $('.box');        var boxHeight = {            leftBox:[],            centerBox:[],            rightBox:[]        };        for(var i=0; i<box.length; i++) {            var now = i%4;  //列数            var now2 = Math.floor(i/4);  //行数            switch(now) {                case 0:                    box.eq(i).css('left','30px');                    boxHeight.leftBox.push(box.eq(i).height());                    if(now2 == 0) {                        box.eq(i).css('top','0px');                    }else {                        var total = 0;                        for(var j=0; j<now2; j++) {                            total += boxHeight.leftBox[j] + 10;                            box.eq(i).css('top',total+'px');                        }                    }                    height1=$("#"+(i+1)).attr('height')+total;//console.log(height1);                    // console.log($("#"+(i+1)).attr('height'));                    // console.log(now+"-"+now2+"-"+total);                    break;                case 1:                    box.eq(i).css('left','260px');                    boxHeight.centerBox.push(box.eq(i).height());                    if(now2 == 0) {                        box.eq(i).css('top','0px');                    }else {                        var total = 0;                        for(var j=0; j<now2; j++) {                            total += boxHeight.centerBox[j] + 10;                            box.eq(i).css('top',total+'px');                        }                    }                    height2=$("#"+(i+1)).attr('height')+total;//console.log(height2);                    // console.log($("#"+(i+1)).attr('height'));                    // console.log(now+"-"+now2+"-"+total);                    break;                case 2:                    box.eq(i).css('left','490px');                    boxHeight.rightBox.push(box.eq(i).height());                    if(now2 == 0) {                        box.eq(i).css('top','0px');                    }else {                        var total = 0;                        for(var j=0; j<now2; j++) {                            total += boxHeight.rightBox[j] + 10;                            box.eq(i).css('top',total+'px');                        }                    }                    height3=$("#"+(i+1)).attr('height')+total;//console.log(height3);                    // console.log($("#"+(i+1)).attr('height'));                    // console.log(now+"-"+now2+"-"+total);                    break;                case 3:                    box.eq(i).css('left','720px');                    boxHeight.rightBox.push(box.eq(i).height());                    if(now2 == 0) {                        box.eq(i).css('top','0px');                    }else {                        var total = 0;                        for(var j=0; j<now2; j++) {                            total += boxHeight.rightBox[j] + 10;                            box.eq(i).css('top',total+'px');                        }                    }                    height4=$("#"+(i+1)).attr('height')+total;//console.log(height4);                    // console.log($("#"+(i+1)).attr('height'));                    // console.log(now+"-"+now2+"-"+total);                    break;            }//        }        h = ((height1>height2?height1:height2)>(height3>height4?height3:height4))?(height1>height2?height1:height2):(height3>height4?height3:height4);        // console.log(h);        $("#water").height(h+10);    });});